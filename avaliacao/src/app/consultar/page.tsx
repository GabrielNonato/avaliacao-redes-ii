import { Button, Container, Typography } from "@mui/material";

export default function Page    () {
  return (
    <Container sx={{mt:'3%'}}>  
      <Typography fontWeight={750} fontSize={24}>
        Consultar Notas
      </Typography>
    </Container>
  );
}
